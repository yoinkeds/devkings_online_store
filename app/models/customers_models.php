<?php
class Customers {
    protected $tableName;
    
    // Checking if users exists and if has an active account
    function if_Exists($tableName, $email){

    }

    // Create / register new user
    function registerUSer($tableName, $userData){

    }

    // Log the user in
    function login($tableName, $email, $password){

    }
    // selectSingle (where ? = ?) params array

    // createNew (table fields)

    // updateSingle (table fields) (set to )+ (where ? = ?) params array
    // bulkUpdate

    // deleteSingle (where ? = ?) params array
    // bulkDelete
}
